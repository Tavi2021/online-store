

function validateForm() {
    let x = document.forms["product_form"]["dimensions"].value; ["product_form"]["weight"].value; ["product_form"]["size"].value;
    if (x == "") {
      Error("Please, provide the data indicated type");
      return false;
    } else validateForm()
  }


saveEvent(Event) {
  var button1Clicked = 
}


var saveButton = document.getElementsByClassName('btn btn-primary btn-xl')
for ( var i=0; i < saveButton.length; i++) {
  var button1 = saveButton[i]
  button1.addEventListener('click', saveEvent(Event) {
    var button1Clicked = EventTarget,
  });


var button1Clicked = EventTarget;




  